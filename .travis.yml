sudo: required
language: python
python: "3.7"

install:
  - pip install tensorflow keras matplotlib

jobs:
  include:
    - name: "tests"
      script:
        - cd tests
        - python -m unittest -v --locals networks_work_test
        - python -m unittest -v --locals poker_logic_test

notifications:
    email: false
